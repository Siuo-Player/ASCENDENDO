# dev_log.txt — Diário de Desenvolvimento
# FICHEIRO APPEND-ONLY: nunca editar entradas anteriores.
#
# ESQUEMA DE VERSÃO DO PROJETO
# ─────────────────────────────────────────────────────────────────────────────
# Formato: vX.Y[Z...]
#   X    = Fase principal (1–8, conforme README)
#   .Y   = Sub-passo da fase  (ex: 2.3 = Fase 2, Passo 3)
#   .YZ  = Pequena atualização dentro de um sub-passo (ex: 2.31 = 1ª fix em 2.3)
#   .YZW = Atualização ainda mais granular (ex: 2.310, 2.311...)
# Regra: cada ficheiro tem @version no seu cabeçalho a refletir quando foi
#        modificado pela última vez. Incrementar sempre que houver mudança.
# ─────────────────────────────────────────────────────────────────────────────
# Versão atual do projeto: 6.4
# ─────────────────────────────────────────────────────────────────────────────
# Formato de entrada:
#   [YYYY-MM-DD HH:MM:SS]
#   Ideia: [O que vamos implementar e o comportamento esperado]
#   Implementado: [Se funcionou, como foi feito, problemas e soluções]
# ════════════════════════════════════════════════════════════════════════

[2026-06-05 00:00:00]
Ideia: v1.0 — Fase 1: Configuracao inicial do ambiente de desenvolvimento.
  1. Estrutura de pastas definitiva conforme arquitetura do README.
  2. Makefile cross-platform (Windows Git Bash/MSYS2 + Linux) com Clang++/C++20.
  3. deps.py: verificador de dependencias com pedido explícito de permissao antes
     de qualquer download/instalacao.
  4. doctest (header-only, MIT, zero instalacao) como framework de testes.
     Justificacao: compilacao ultrarrápida, interface familiar, SUBCASE nativo.
  5. Hook pre-commit que bloqueia commits com testes a falhar.
  6. .gitignore adequado para C++/Clang/Vulkan.
  7. Testes de sanidade: framework operacional + sizeof de tipos fundamentais.
     (critico para determinismo de fisica e integridade de save states).
Implementado: Setup validado (clang++ 18, doctest 2.5.0).
  Makefile, deps.py, test_runner.cpp, test_placeholder.cpp, doctest.h,
  pre-commit.sh, .gitignore criados. Resultado:
  make tests -> 2/2 passed | 9/9 assertions | Status: SUCCESS

[2026-06-05 23:50:00]
Ideia: v1.0 — Fix: PATH do sistema para clang++.
Implementado: LLVM instalado em C:\Program Files\LLVM\bin mas sem PATH.
  Fix via PowerShell Admin:
    [Environment]::SetEnvironmentVariable("PATH", "$p;C:\Program Files\LLVM\bin", "Machine")
  Resultado: clang++ 22.1.6 detetado. make tests -> SUCCESS.
  FASE 1 OFICIALMENTE CONCLUIDA.

[2026-06-06 00:00:00]
Ideia: v2.1 — Fase 2.1: Integracao do Vulkan SDK (headless, TDD puro).
  Objetivo: provar que o SDK esta corretamente ligado e que a GPU responde a
  pedidos Vulkan basicos sem qualquer janela ou swapchain.
  Testes (Tests/Integration/test_vulkan_init.cpp):
    1. VkInstance: criacao e destruicao sem erros
    2. Driver suporta Vulkan >= 1.3
    3. Pelo menos 1 GPU Vulkan detetada (lista nome e tipo)
  Makefile: ativar secao VULKAN_SDK (INCLUDES + LDFLAGS para vulkan-1).
Implementado: Makefile atualizado com secao Vulkan descomentada.
  test_vulkan_init.cpp criado com helper createTestInstance() reutilizavel.
  Problema: AR (llvm-ar) com espacos no path "Program Files" partia o comando.
  Solucao: AR := $(shell command -v llvm-ar > /dev/null 2>&1 && echo llvm-ar || echo ar)
  (retorna o NOME, nao o caminho completo).
  Resultado: 3/3 testes Vulkan a passar. GPU detetada: NVIDIA (discreta).

[2026-06-06 01:00:00]
Ideia: v2.2 — Fase 2.2: VulkanContext — primeira abstracao da camada grafica.
  Encapsula VkInstance + VkPhysicalDevice + VkDevice + fila grafica.
  Interface init(bool)/shutdown() testavel (retorna bool, sem excecoes).
  Selecao de GPU: prefere discreta, fallback para integrada.
  namespace gfx:: para toda a camada grafica.
  Testes (Tests/Unit/test_vulkan_context.cpp):
    - init() devolve handles validos
    - GPU selecionada tem nome nao vazio
    - shutdown() e idempotente (2x sem crash)
    - contexto nao inicializado -> VK_NULL_HANDLE
    - init() e idempotente (2x sem recriar)
Implementado: VulkanContext.h/.cpp criados (v2.2). 5 testes a passar.
  Total acumulado: 2 + 3 + 5 = 10 testes.

[2026-06-06 02:00:00]
Ideia: v2.3 — Fase 2.3: Window (GLFW) + Surface Vulkan.
  Dependencia nova: GLFW 3.4 (windowing cross-platform para Vulkan).
  Justificacao: mais leve e direta que SDL2 para apenas windowing;
  SDL2 fica para audio/input avancado (Fase 3+). Licenca zlib/libpng.
  Instalacao: glfw-3.4.bin.WIN64.zip -> external/glfw/ (include/ + lib-vc2022/).
  Estrategia compilacao condicional:
    GLFW_AVAILABLE = auto-detetado pelo Makefile via $(wildcard).
    Sem GLFW: Window compila como stub (create=false). Testes inativos.
    Com GLFW: Window real + 4 testes de integracao ativos.
  VulkanContext: init() agora aceita instanceExtensions (necessario para surface).
  Surface criada pelo Window e passada via VulkanContext::createSurface().
  c_cpp_properties.json criado para eliminar erros falsos do IntelliSense.
Implementado: Window.h/.cpp (v2.3), VulkanContext.h/.cpp atualizados (v2.3).
  -lshell32 adicionado (necessario para GLFW 3.4 no Windows).
  4 testes Window + Surface a passar. Total: 14 testes.

[2026-06-06 03:00:00]
Ideia: v2.4 — Fase 2.4: Swapchain (double buffering + V-Sync).
  Swapchain.h/.cpp: encapsula VkSwapchainKHR, formato e extent.
  Formato: VK_FORMAT_B8G8R8A8_SRGB (universalmente suportado em desktop).
  Apresentacao: VK_PRESENT_MODE_FIFO_KHR (V-Sync garantido, sem tearing).
  VulkanContext.cpp: VK_KHR_SWAPCHAIN_EXTENSION_NAME sempre ativo no device.
  Teste: cria cadeia Window -> Vulkan -> Surface -> Swapchain, verifica extent.
  Nota futura (v2.40): validar com vkGetPhysicalDeviceSurfaceCapabilitiesKHR
  para escolher formato e preTransform dinamicamente.
Implementado: Swapchain.h/.cpp (v2.4), test_swapchain.cpp criado.
  Swapchain 800x600 criado e destruido sem erros. Total: 15 testes.

[2026-06-06 04:00:00]
Ideia: v2.5 — Fase 2.5: RenderPass (define clear + store por frame).
  RenderPass.h/.cpp: 1 color attachment, 1 subpass grafico.
  loadOp  = CLEAR  (limpa o ecra a cada frame — cor definida no render loop).
  storeOp = STORE  (guarda resultado para apresentacao no monitor).
  finalLayout = VK_IMAGE_LAYOUT_PRESENT_SRC_KHR (pronto para o ecra).
  Dependency de sincronizacao: garante que o RenderPass espera pelo color output.
  Teste: cria cadeia completa Window->Vulkan->Surface->Swapchain->RenderPass.
  reorganize.py refatorizado: modo silencioso, substituicao inteligente.
  Makefile: target tests-verbose adicionado (make tests-verbose para debug).
Implementado: RenderPass.h/.cpp (v2.5), test_render_pass.cpp criado.
  Pipeline de inicializacao validada ate Fase 2.5. Total: 16 testes a 100%.
  VERSAO DO PROJETO: 2.5

[2026-06-06 05:00:00]
Ideia: v2.6 — Fase 2.6: Renderer — Framebuffers + CommandBuffers + Sync + drawFrame.
  Objetivo: primeiro ecrã visível (janela colorida). Fim da Fase 2 do motor.
  Swapchain atualizado (v2.40): adicionar image views (necessario para Framebuffers).
  Renderer.h/.cpp: Framebuffers (1 por imagem do swapchain), Command Pool,
    Command Buffers, Semáforos (imageAvailable, renderFinished) e Fence (inFlight).
  drawFrame(r,g,b): acquire → gravar clear → submit → present.
  Fence criada sinalizada (VK_FENCE_CREATE_SIGNALED_BIT) — evita espera infinita no 1o frame.
  test_renderer.cpp: mini render loop de 500 frames (~8s) a azul escuro.
    CHECK(successFrames == 500) verifica que todos os frames foram produzidos.
  Versao do projeto apos esta fase: 2.6
Implementado: 40/40 testes a passar | 132 assertions | Status: SUCCESS.
  (36 previstos + 4 extras detectados no verbose — contagem correcta: 40)

[2026-06-06 06:00:00]
Ideia: v2.60 — Fix: test_renderer.cpp — janela fecha automaticamente.
Implementado: Utilizador fechou a janela manualmente ao ver o titulo "Primeiro Frame!",
  causando shouldClose()=true no frame 350 e CHECK(350 == 500) a falhar.
  Fix: remover '&& !win.shouldClose()' do loop do teste.
  O teste abre a janela, renderiza 500 frames (~8s) e fecha automaticamente.
  Titulo da janela atualizado: "ASCENDENDO v2.6 | NAO FECHAR — fecha automaticamente".

[2026-06-06 07:00:00]
Ideia: v2.6 — Concluido. Resultado: 18/18 testes a passar.
  Motor grafico base operacional: janela azul escuro renderizada durante 500 frames.
  Fix v2.60: test_renderer.cpp — remover shouldClose() do loop (utilizador fechou
  a janela manualmente, loop saiu no frame 350, CHECK falhou).
  FASE 2 OFICIALMENTE CONCLUIDA.
Implementado: Confirmado com make tests-verbose. Commit e push feitos.

[2026-06-06 08:00:00]
Ideia: v3.1 — Fase 3: Input + Fisica Deterministica.
  .gitattributes criado: text=auto para elimininar avisos LF/CRLF no Windows.
  InputManager.h/.cpp (Game/Logic/):
    - onKeyEvent(key, action): injecao de eventos (testavel sem GLFW)
    - beginFrame(): limpa justDown/justUp no inicio de cada frame
    - isKeyDown, isKeyJustPressed, isKeyJustReleased
    - isLeft(), isRight(), isJump() (A/LEFT, D/RIGHT, SPACE)
    - registerWithWindow(): liga ao callback GLFW em producao
  Physics.h/.cpp (Game/Logic/):
    - Vec2: soma, subtracao, escalar, operador+=
    - AABB: overlaps() (Separating Axis Theorem simplificado)
    - PhysicsBody: posicao, velocidade, bounds() -> AABB
    - PhysicsWorld: step() (gravidade + Euler + colisao chao),
      jump() (so se isGrounded), collides(), advance() (Fixed Timestep)
    - Constantes: GRAVITY=-980 px/s², FIXED_STEP=1/60s, GROUND_Y=0
  test_input.cpp: 7 testes unitarios (sem GLFW — injecao de eventos)
  test_physics.cpp: 11 testes unitarios (Vec2, AABB, step, jump, Fixed Timestep)
Implementado: 40/40 testes a passar | 132 assertions | Status: SUCCESS.
  (36 previstos + 4 extras detectados no verbose — contagem correcta: 40)

[2026-06-06 09:00:00]
Ideia: v3.2 — Fase 3.2: Player — integracao Input + Physics.
  Player.h/.cpp (Game/Logic/):
    - MOVE_SPEED = 200 px/s (horizontal, sem aceleracao por agora)
    - MIN_JUMP_FORCE = 300 px/s | MAX_JUMP_FORCE = 750 px/s | CHARGE_TIME = 0.5s
    - update(input, world, dt): horizontal -> carga -> world.step()
    - Toque rapido (press+release no mesmo frame) -> MIN_JUMP_FORCE
    - Hold SPACE no chao -> acumula jumpCharge [0..1] -> release -> interpola forca
    - Salto no ar ignorado
    - chargeRatio() exposto para barra de UI (Fase 6)
  test_player.cpp: helpers tap/frameWith/stepN para simular game loop correcto.
  Testes de movimento: D, A, A+D, sem teclas.
  Testes de salto: toque rapido, carga total, salto no ar, limite 1.0, 2 saltos comparados.
Implementado: [A preencher apos make tests]

[2026-06-09 16:21:00]
Ideia: v3.3 — Fase 3.3: Save States e Replay System.
  Objetivo: Gravar o estado da simulacao e inputs por frame para permitir rewind/replay deterministico.
  Estruturas (Game/Logic/ReplayManager.h):
    - FrameInput: abstrai o estado das teclas logicas (left, right, jump).
    - GameState: encapsula copia do Player e PhysicsWorld::m_accumulator.
    - ReplayManager: guarda historico e gerencia record(), rollback() e playFrame().
  PhysicsWorld.h atualizado: m_accumulator e setAccumulator() expostos para o GameState.
  Testes (Tests/Unit/test_replay.cpp):
    - Gravar e restaurar estado exato do Player (posicao, velocidade, carga).
    - Gravar e reproduzir sequencia de inputs.
    - Garantir determinismo: restaurar state + re-simular gera o mesmo resultado.
Implementado: [A preencher apos make tests]

# NOTA PARA A FASE 4 (Level Streaming e Viewport):
# A resolucao logica interna do jogo (pixel-art) sera 360x640 (aspect ratio 9:16).
# O Vulkan vai renderizar tudo para um Offscreen Framebuffer de 360x640.
# A classe Window (GLFW) pode ser redimensionada livremente pelo utilizador.
# A classe Renderer ficara encarregue de fazer o "Integer Upscaling" do framebuffer
# interno para o Swapchain, preenchendo o espaco restante com letterbox (barras pretas)
# para manter os pixeis perfeitos e o determinismo da fisica intacto.

[2026-06-09 18:25:00]
Ideia: v4.1 — Fase 4.1: Camara 2D e Sistema de Coordenadas.
  Objetivo: Separar o espaco de simulacao (logico) da resolucao do ecra.
  Resolucao logica alvo: 360x640 (aspect ratio 9:16).
  Estruturas (Game/Graphics/Camera.h):
    - Camera: define posicao (world space) e limites logicos.
    - Converte posicoes do Mundo para Normalized Device Coordinates (NDC) do Vulkan.
  Testes (Tests/Unit/test_camera.cpp):
    - Validar projecao de world space para NDC (Y invertido para Vulkan).
    - Mapeamento correto das bordas do viewport.
Implementado: [A preencher apos make tests]

[2026-06-09 19:30:00]
Ideia: v5.1 — Fase 5.1: Graphics Pipeline e Shaders GLSL.
  Objetivo: Escrever os shaders base e instruir o Makefile a compilar o código GLSL para SPIR-V.
  Letterboxing nativo: Em vez de Offscreen Framebuffers, a Pipeline usará VkViewport e VkScissor dinâmicos para trancar a área de desenho aos exatos 360x640 (aspect ratio 9:16) no centro do ecrã.
  Novos Ficheiros:
    - Game/Assets/Shaders/base.vert (Vertex Shader c/ Push Constants)
    - Game/Assets/Shaders/base.frag (Fragment Shader)
    - Atualização do Makefile para usar o `glslc` do Vulkan SDK.
Implementado: [A preencher apos make tests]

[2026-06-09 20:30:00]
Ideia: v5.2 e v5.3 — Arquitetura Global e A Ponte (Física -> Gráficos).
  Objetivo: Criar ficheiro de Configurações Globais (Config.h) para eliminar magic numbers, 
  e desenhar o Player em tempo real usando a Graphics Pipeline.
  Implementação:
    - Game/Core/Config.h: LOGICAL_WIDTH (640), LOGICAL_HEIGHT (360), FIXED_STEP.
    - Shaders leem dinamicamente a resolução via Push Constants.
    - Renderer agora recebe const Player& e const Camera& no drawFrame.
    - Teste de Integração (test_renderer) simula 120 frames de queda livre do Player.
Implementado: 54/54 testes a passar. O quadrado cai e colide perfeitamente com Y=0.

[2026-06-09 21:00:00]
Ideia: v6.1 — Salto Parabólico com Inércia (Commitment Jump).
  Objetivo: O jogador não pode mudar de direção a meio do ar. O ângulo de salto é ditado por 60º.
Implementado: main.cpp protegido com RAII no loop Vulkan. test_player.cpp totalmente refatorizado para as novas mecânicas. Funcional e testado.

[2026-06-09 21:30:00]
Ideia: v6.1 — Salto Parabólico com Inércia (Commitment Jump).
  Objetivo: O jogador não pode mudar de direção a meio do ar. O ângulo de salto é ditado por 60º.
Implementado: main.cpp criado com RAII no loop Vulkan. test_player.cpp adaptado para validar bloqueio aéreo de inputs e correção de aplicação instantânea de gravidade (-16.333/frame).
[2026-06-10 00:00:00]
Ideia: v6.2 — Fase 6.2: Sistema de Plataformas (Level class, logica pura).
  Level.h/.cpp (Game/Logic/):
    - struct Platform: apenas AABB de colisao (cor fica para Renderer na 6.2b)
    - Level::addPlatform(x,y,w,h): canto inferior-esquerdo + dimensoes
    - Level::resolveCollision(PhysicsBody&): pousa no TOPO (velocity.y<=0), one-way
    - SKIN_WIDTH=1px: resolve persistencia de isGrounded sem tocar em Physics.cpp
    - Retrocompativel: step() inalterado, todos os testes anteriores intocados
  test_level.cpp (Tests/Unit/): 10 novos testes unitarios sem GPU.
    - Construcao: nivel vazio, addPlatform bounds (min/max/width/height),
      multiplas na ordem de insercao, clear()
    - Colisao: pousar no topo, permanecer N frames, one-way (subida atravessa),
      X fora do alcance cai ao chao, nivel vazio nao interfere
    - Fisica combinada: plataforma mais alta intercepta antes do chao,
      corpo cai ao sair lateralmente da borda
Implementado: 58/58 testes a passar | 201 assertions | Status: SUCCESS.
  FASE 6.2 CONCLUIDA.

[2026-06-10 01:00:00]
Ideia: v6.2b — Fase 6.2b: Renderizacao de Plataformas (Renderer integra Level).
  Renderer.h/.cpp (v6.2b):
    - drawFrame() aceita const logic::Level* level = nullptr (retrocompativel).
    - recordCommandBuffer() desenha plataformas ANTES do jogador (camada de fundo).
    - Cor plataformas: verde escuro (0.35, 0.65, 0.25).
  main.cpp (v6.2b):
    - Level criado com 4 plataformas de teste dispostas em altura crescente.
    - Loop: player.update() + level.resolveCollision() + drawFrame(&level).
    - Jogador inicia em (320, 400) e cai para as plataformas visiveis.
Implementado: [A preencher apos make game]

[2026-06-11 00:00:00]
Ideia: v6.2c — Fase 6.2c: Correcao de 3 bugs de fisica reportados apos teste.
  Bug 1 — Colisao lateral: jogador atravessava plataformas pelo lado.
    Fix: Level::resolveCollision() reescrita com MTV (Minimum Translation Vector).
    Caso 1 (skin overlap): comportamento de topo inalterado.
    Caso 2 (sobreposicao real): |vx| > |vy| → colisao lateral (empurra X);
    caso contrario → colisao vertical: a cair pousa no topo, a subir passa (one-way).
  Bug 2 — Paredes absolutas: sem limite esquerda/direita, jogador saia do nivel.
    Fix: PhysicsWorld::step() agora clampeia X a [0, LOGICAL_WIDTH - body.width]
    usando config::LOGICAL_WIDTH. Chao Y=0 ja existia; paredes laterais adicionadas.
  Bug 3 — Queda de borda com trajectória parabólica indesejada.
    Fix: Player::m_didJump distingue salto intencional (SPACE) de queda de borda.
    Queda de borda (m_didJump=false no ar) → velocity.x=0, cai a pique.
    Salto intencional (m_didJump=true) → mantém inércia (Commitment Jump intacto).
  Testes novos:
    test_level.cpp: +3 (colisao lateral esquerda, direita, queda nao empurra).
    test_player.cpp: +2 (cair de borda zera vel.x, salto mantém inércia).
  Testes antigos intocados: comportamento one-way mantido (one-way from below).
Implementado: [A preencher apos make tests]

[2026-06-11 01:00:00]
Ideia: v6.3 — Barra de Forca Visual (UI sobre o jogador).
  Renderer.cpp (v6.3): 2 draw calls adicionais quando player.isCharging=true.
    - Fundo cinza escuro: largura=player.width, altura=6px, 8px acima da cabeca.
    - Preenchimento: largura=player.width*chargeRatio, cor interpola verde→vermelho.
  Sem novos testes unitarios (comportamento puramente visual).
Implementado: 63/63 testes a passar | 215 assertions | Status: SUCCESS.
  FASE 6.3 CONCLUIDA.

[2026-06-11 02:00:00]
Ideia: v6.4 — Camera Tracking Vertical.
  Objetivo: A camera segue o jogador em Y para que ele nao saia do ecra ao saltar.
  Camera.h/.cpp (v6.4):
    - Camera::follow(target, dt, speed=5.0): Lerp suave em Y.
    - Offset fixo de 126px (LOGICAL_HEIGHT*0.35): jogador no 35% inferior do ecra,
      mostrando mais plataformas acima (melhor para jogo vertical).
    - Camera Y nunca vai abaixo de 0 (chao absoluto).
    - Camera X inalterado (o jogo tem paredes laterais absolutas).
  main.cpp (v6.4): camera.follow(player.position(), dt) apos o loop de fisica.
    - Quinta plataforma adicionada ao nivel de demo (Y=320).
  test_camera.cpp (v6.4): ficheiro reconstruido + 3 novos testes (follow).
    - follow() converge para alvo apos 2s.
    - follow() nao vai abaixo de Y=0.
    - follow() nao afeta camera.position.x.
  Fix: test_level.cpp — removidas variaveis nao usadas (warning do compilador).
Implementado: [A preencher apos make tests]

[2026-06-19 15:00:00]
Ideia: v7.3 — Fix Crítico: Validator V_MAX Mismatch + Redesign de Níveis.
  DIAGNÓSTICO: Campanha impossível de completar porque ai_validator.py usava
  V_MAX=800 mas Config.h v7.2 define PLAYER_MAX_JUMP=600. Consequência:
    - Jogo:       max altura = (600×sin60°)² / (2×980) = 137.7 px
    - Validator:  max altura = (800×0.85×sin60°)² / (2×980) = 176.9 px
    - zigzag.lvl P0→P1 exige 145 px → IMPOSSÍVEL no jogo (137.7 < 145)
  CORREÇÃO 1: ai_validator.py + reorganize.py
    - V_MAX: 800 → 600 (alinhado com PLAYER_MAX_JUMP em Config.h)
    - TOLERANCE: 0.85 → 0.90 (nível passável com 90% de carga)
    - Novo max validador: 111.6 px
  CORREÇÃO 2: Objetivo de níveis de trânsito (sem FLAG)
    - Antes: BFS tentava atingir y=640 — impossível com plataformas em ~y=505
    - Agora: BFS usa (topo_da_plataforma_mais_alta + 50) como saída
    - Semanticamente correto: o jogo carrega o próximo chunk quando o jogador
      sobe acima de currentSpawnY - LOGICAL_HEIGHT (não precisa atingir y=640)
  REDESIGN DOS NÍVEIS (todos verificados matematicamente com novo validador):
    inicio.lvl: plataformas largas (180px) em ZigZag L↔R. Gaps: 85-100px.
      P0→P1→P2→P3→P4→P5 (5 saltos, topo em y=505)
    zigzag.lvl: plataformas médias (160px), gap horizontal real de 40px. Gaps: 95-110px.
      P0→P1→P2→P3→P4→P5 (5 saltos, topo em y=555)
    precipicio.lvl: plataformas estreitas (100px), gap horizontal de 60px. Gaps: 100-110px.
      P0→P1→P2→P3→P4→FLAG (5 saltos + flag em y=510)
  GUIA DE JOGO (Commitment Jump 60°):
    - Cada plataforma está ALTERNADA: direita→esquerda→direita
    - Para alcançar P1 (lado DIREITO), o jogador está em x=320, SALTA PARA A ESQUERDA
    - Para alcançar P2 (lado ESQUERDO), o jogador vai para a borda DIREITA de P1 e SALTA
    - A mecânica de 60° significa: salta sempre do lado OPOSTO ao alvo
Implementado: [A preencher após make tests + jogar a campanha]

[2026-06-19 16:30:00]
Ideia: v7.3/7.4 — Fix Crítico: Streaming Quebrado + Spawn Dentro da Plataforma.
  REPORT DO RAFAEL: "ele não carrega o proximo nivel" + "a primeira plataforma
  fica com o jogador dentro dela" + "as alturas devem ser padronizadas, todos
  os niveis têm no máximo uma tela de altura (360)".

  DIAGNÓSTICO 1 — Streaming quebrado:
    Level::appendFromFile retornava (highestY + 50), um valor que dependia do
    CONTEÚDO de cada nível (variável: ~505-570px nos níveis antigos). O trigger
    em main.cpp (`player.y > currentSpawnY - LOGICAL_HEIGHT`) assume blocos de
    altura FIXA — com avanço variável, o próximo chunk só carregava muito tarde
    (ou em momentos imprevisíveis), dando a sensação de "não carrega".
  CORREÇÃO: Level.cpp — appendFromFile agora retorna SEMPRE
    `offsetY + config::LOGICAL_HEIGHT` (360px), independente do conteúdo.
    Cada nível = exactamente uma tela. Chunk N ocupa sempre
    [N*360, (N+1)*360) no mundo. Aviso (cerr) se alguma PLATFORM/FLAG
    ultrapassar essa altura (nível mal desenhado).
    Resultado colateral automático: o trigger de streaming passa a disparar
    "assim que o jogador entra no nível anterior" (exactamente o pedido do
    Rafael), porque agora currentSpawnY-LOGICAL_HEIGHT == início do chunk atual.

  DIAGNÓSTICO 2 — Jogador dentro da plataforma:
    main.cpp definia player.body.position.y = 0.0f, mas a primeira PLATFORM de
    cada nível ocupa Y=[0,20]. O corpo do jogador (16px) nascia inteiramente
    DENTRO da plataforma.
  CORREÇÃO: main.cpp — spawn agora em Y=40.0f (queda visível de 20px até
    pousar no chão do chunk 0).

  DIAGNÓSTICO 3 — Validadores Python com constantes trocadas:
    ai_validator.py tinha LOGICAL_WIDTH=360 e LOGICAL_HEIGHT=640 — TROCADOS
    em relação ao motor real (640 largura × 360 altura, paisagem). Isto:
      a) Bloqueava plataformas de largura total (640px) por engano (o limite
         lateral usado era 360, nao 640) — usado no reorganize.py.
      b) BFS de níveis de trânsito tentava provar alcançar Y=640 (LOGICAL_HEIGHT
         errado), quando devia ser Y=360.
  CORREÇÃO: ai_validator.py, reorganize.py (check_level_validity embutido),
    level_validator.py — LOGICAL_WIDTH=640.0, LOGICAL_HEIGHT=360.0 em todos.
    Removido o workaround "objetivo = topo_mais_alto+50" (já não é necessário
    com chunks padronizados); revertido para o objetivo simples original
    "alcançar Y=LOGICAL_HEIGHT". Adicionada validação HARD FAIL: nenhuma
    PLATFORM/FLAG pode ultrapassar uma tela de altura (y+h <= 360).

  REDESIGN DOS 3 NÍVEIS (todos re-verificados com os validadores corrigidos):
    Estrutura padrão: chão (PLATFORM 1) com LARGURA TOTAL (640px) — pouso
    seguro garantido em qualquer X — seguido de 3 plataformas de salto em
    Y locais 95/195/295, todas dentro de uma tela (max topo=315, margem de
    45px até Y=360).
    inicio.lvl:      plataformas largas (180px), gaps horizontais de 20px.
    zigzag.lvl:      plataformas médias (140px), gaps horizontais de 40px.
    precipicio.lvl:  plataformas estreitas (100px), gaps de 60px, termina
                      com FLAG (em vez do 3º salto de trânsito).

  test_level.cpp (v7.4): teste de stacking corrigido (360, não 165 — refletindo
    o novo avanço padronizado); FLAG de teste movida para Y=300 (dentro de uma
    tela); +1 teste de regressão explícito ("nivel vazio e nivel cheio avançam
    o MESMO valor de chunk") capturando directamente o bug corrigido.
    Total: 16 testes em test_level; 67 testes globais.
Implementado: [A preencher após make tests + jogar a campanha completa]

[2026-06-24 18:00:00]
Ideia: v7.4 — Niveis gerados por simulacao fisica real + integracao ai_validator como doctest.
  PROBLEMAS DIAGNOSTICADOS E RESOLVIDOS:
  1. Niveis anteriores exigiam quase carga maxima (90-100%) porque gap=95px era demasiado.
     Com ângulo fixo de 60°, o corpo percorre sempre >=66px horizontais para ganhar 85px
     de altura — niveis com gap<66px nao sao alcancaveis, gap muito grande aproxima-se
     de 100% de carga (janela estreita, pixel-perfect).
  2. Bug de colisao lateral na SUBIDA: corpo a saltar para a esquerda entrava na zona X
     da plataforma alvo enquanto ainda subia. abs(vx)>abs(vy) → motor detectava colisao
     LATERAL → ricochete → corpo perdia-se. Resolvido com ascending_x_at() que calcula
     onde o corpo esta na subida e garante que a borda proxima da plataforma fica 40px
     PARA LA desse ponto (clearance=40px).
  3. Cross-chunk dy=105 (nao 85): o ultimo platform de cada nivel fica em local_top=275.
     O primeiro do nivel seguinte fica em local_top=20 (offset+20 no mundo). Diferenca
     = (N+1)*360+20 - (N*360+275) = 105px. SEMPRE 105px cross-chunk. Minimo de carga
     para dy=105 e 78.3%. Usando charge_cross=92% (minimo noisy=84.6%, bem acima).
  4. start_x para chao de largura total usava borda direita (x=624), batia na parede.
     Corrigido para usar o centro (x=312).

  ALGORITMO FINAL (levelgen.py v4):
    Para cada salto:
      1. ascending_x_at(): calcula x do corpo na SUBIDA ao atingir a altura alvo (analitico).
      2. fly_descend_to(): simula voo completo, retorna x quando DESCE ate altura alvo.
      3. place_platform(): centra na aterragem; garante borda proxima <= x_asc - 40px.
      4. Verifica deterministicamente + 50 trials com +-8% carga e +-10px posicao.
    Charges: intra-nivel=78%, cross-chunk=92%.
    Resultado: 50/50 (100%) em todos os 11 saltos da campanha.

  NIVEIS GERADOS (coordenadas locais por chunk):
    inicio.lvl:      PLATFORM 0 0 640 20 | P1=[436,606] | P2=[148,328] | P3=[436,616]
    zigzag.lvl:      entry=[170,310] | P2=[418,558] | P3=[170,310] | P4=[418,558]
    precipicio.lvl:  entry=[181,281] | P2=[389,489] | P3=[181,281] | FLAG=[389,509] h=40

  DOCTEST INTEGRATION (test_campaign.cpp v7.4):
    Novo TEST_CASE chama python3 ai_validator.py --campaign e verifica ret==0.
    O pre-commit corre 'make tests', que inclui este teste.
    Se os niveis forem invalidos, o commit e bloqueado automaticamente.

  REORGANIZE.PY: test_campaign.cpp adicionado a MOVES (→ Tests/Unit/).
Implementado: [A confirmar com make tests + jogar campanha]

# ─────────────────────────────────────────────────────────────────────────────
# Versao atual do projeto: 7.4
# ─────────────────────────────────────────────────────────────────────────────

[2026-06-25 10:00:00]
Ideia: v7.4 — Fix: duplicate symbol em test_campaign.cpp + UnicodeEncodeError no Windows.
  PROBLEMA 1 — duplicate symbol ao linkar:
    test_campaign.cpp tinha '#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN', que so'
    pode existir num unico ficheiro de todo o projeto (Tests/test_runner.cpp).
    Todos os outros ficheiros de teste incluem apenas '#include "doctest/doctest.h"'.
  CORRECAO: removida a macro de test_campaign.cpp. Apenas TEST_SUITE + TEST_CASE.

  PROBLEMA 2 — UnicodeEncodeError no Windows (cp1252):
    ai_validator.py usava emojis (checkmark, X) no output. O Windows com codepage
    cp1252 nao suporta esses caracteres, causando crash do script com retorno != 0,
    o que fazia o TEST_CASE do doctest falhar mesmo quando os niveis eram validos.
  CORRECAO: ai_validator.py reescrito sem emojis (usa [OK]/[ERRO] ASCII).
    Adicionado 'sys.stdout.reconfigure(encoding="utf-8", errors="replace")' no topo
    de ai_validator.py E reorganize.py para segurança cross-platform.

  REORGANIZE.PY atualizado:
    Novos MOVES:
      engine.py, solver.py, flag_solver.py, levelgen.py
        → Development/AI_Validation/sim/
      level_validator.py → Development/LevelEditor/ (remove duplicado da raiz)
      log_testes.txt     → Tests/log_testes.txt
      pre-push.sh        → scripts/pre-push.sh
    Novos DIRS_WITH_GITKEEP:
      Development/AI_Validation/sim/
      Game/Assets/Levels/NaoValidados/ (preservado)
      Game/Assets/Levels/Unused/       (preservado)

  VERSAO ATUAL: 7.4 — 68 testes esperados (67 + test_campaign).
Implementado: [A confirmar com make tests > log_testes.txt 2>&1]

[2026-06-25 11:00:00]
Ideia: v7.4 — Comando canonico para capturar log completo de testes.
  O operador '>' so' redireciona stdout; erros de compilacao iam para stderr
  e nao apareciam em log_testes.txt.
Implementado: Comando canonico:
    python reorganize.py && make clean && make tests-verbose -j8 > log_testes.txt 2>&1
  log_testes.txt passa a residir em Tests/ (adicionado ao reorganize.py MOVES).

[2026-06-29 12:00:00]
Ideia: v7.5 — FLAG visual, plataforma sob FLAG, spawn acima do chao, menu legivel (BitmapFont).
  PROBLEMAS CORRIGIDOS:
  1. FLAG sem representacao fisica: A FLAG era apenas uma zona de colisao
     invisivel. Adicionado visual com mastro (pole, 4px, dourado escuro) +
     pano (pennant, 45% largura, dourado brilhante) + zona de touchdown
     semi-transparente (alpha 0.18). Plataforma solida adicionada em
     precipicio.lvl (PLATFORM 389 215 120 20) mesmo abaixo da FLAG.
  2. Spawn dentro do chao: player.body.position.y = 0.0f colocava o
     jogador DENTRO da primeira plataforma (y=0, top=20). Corrigido
     para y=40.0f (20px acima do topo da plataforma). Removido tambem
     o addPlatform de chao absoluto redundante do resetGame().
  3. Menu ilegivel (so blocos coloridos): Implementada fonte bitmap 5x5
     (BitmapFont.h) com todos os caracteres ASCII imprimiveis, maiusculas
     e minusculas (mapeadas para maiusculas). drawGlyph() + drawText() +
     drawTextC() (centrado) como lambdas locais no recordCommandBuffer.
     CREDITS: texto real com ASCENDENDO, FIM DA CAMPANHA, AUTOR:,
     RAFAEL GOMES BERNARDO, AUXILIADO POR:, CLAUDE ANTHROPIC, GEMINI GOOGLE,
     ESPACO PARA CONTINUAR. MENU: duas caixas com borda (4 rectangulos),
     fundo escuro, label em texto real, seta indicadora, dicas de navegacao.
  4. glfwSetWindowTitle: titulo da janela muda com o estado (CREDITS/MENU)
     para reforcar as instrucoes de controlo.

  FICHEIROS ALTERADOS:
    Game/Graphics/BitmapFont.h  — NOVO: fonte 5x5 (128 chars)
    Game/Graphics/Renderer.h    — v7.5: GameState enum, assinaturas
    Game/Graphics/Renderer.cpp  — v7.5: drawText, FLAG visual, CREDITS/MENU reais
    main.cpp                    — v7.5: spawn y=40, sem addPlatform redundante, titles
    Game/Assets/Levels/precipicio.lvl — PLATFORM 389 215 120 20 (pedestal FLAG)

  NOTA FUTURA (editor de niveis): a FLAG deve sempre ter um PLATFORM logo
  abaixo. O levelgen.py devera garantir isso automaticamente.
Implementado: [A confirmar com make tests && make game]

[2026-07-02 20:00:00]
Ideia: v7.6 — Substituir BitmapFont (pixelado, feedback negativo do Rafael:
  "trocar a escrita meio que pixelada (ainda por cima mal) por fontes
  normais") por texto TTF real, anti-aliased, via stb_truetype.

  DECISAO ARQUITECTURAL: pipeline SEPARADA em vez de modificar Pipeline.h/.cpp
  existente. Razao: Pipeline.h/.cpp e base.vert/base.frag ja estao cobertos
  por 68 testes a passar (incl. test_pipeline.cpp, test_renderer.cpp,
  test_render_pass.cpp que usam a pipeline solida). Adicionar sampling de
  textura aa MESMA pipeline exigiria descriptor sets na PushConstants
  existente, arriscando quebrar esses testes por qualquer erro de
  alinhamento/layout. Uma TextPipeline nova e 100% aditiva: zero linhas
  alteradas em Pipeline.h/.cpp/base.vert/base.frag. As duas pipelines
  partilham o mesmo RenderPass e alternam dentro do mesmo command buffer
  via vkCmdBindPipeline (viewport/scissor dinamicos mantem-se validos
  entre pipelines compativeis — nao e necessario re-programar).

  NOVOS FICHEIROS:
    Game/Graphics/TextPipeline.h/.cpp — pipeline dedicada. Declara um
      VkDescriptorSetLayout (1 combined image sampler, estagio fragment)
      no VkPipelineLayoutCreateInfo. TextPushConstants (56 bytes: color,
      objPos, objSize, uv0, uv1, logicalRes — todos float/float[N], sem
      padding escondido pelo compilador).
    Game/Graphics/FontRenderer.h/.cpp — baking CPU (stb_truetype) + upload
      GPU (staging buffer -> VkImage R8_UNORM -> VkImageView -> VkSampler
      -> VkDescriptorSet). Atlas 512x512, ASCII 32-126 (95 chars), baked a
      48px. drawText()/drawTextCentered()/textWidth() calculam a geometria
      do quad em espaco Y-CIMA (mundo do jogo) a partir dos dados Y-BAIXO
      do stb_truetype (yoff negativo = acima do baseline).
      IMPORTANTE (mesma licao do bug duplicate-symbol do doctest):
      STB_TRUETYPE_IMPLEMENTATION definido APENAS em FontRenderer.cpp.
      FontRenderer.h NAO inclui stb_truetype.h (evita vazar a macro).
    Game/Assets/Shaders/text.vert/text.frag — shaders dedicados, UV mapeado
      para o atlas (V invertido: c.y=0 do quad -> t1 do atlas, V-baixo).
    Game/Assets/Fonts/UIFont.ttf — Orbitron (Google Fonts, licenca OFL),
      geometrica/futurista, adequada ao tema. FACILMENTE SUBSTITUIVEL —
      e so um ficheiro de dados, o codigo nao assume nada sobre o
      conteudo tipografico especifico.
    Tests/Integration/test_text_pipeline.cpp — 2 testes seguindo o padrao
      de test_pipeline.cpp/test_renderer.cpp (VulkanContext real).

  RETROCOMPATIBILIDADE: Renderer::attachText(TextPipeline*, FontRenderer*)
  e um setter OPCIONAL chamado por main.cpp apos ambas as pipelines
  inicializarem. Se falhar (shaders .spv em falta, .ttf em falta), o jogo
  IMPRIME AVISO e cai automaticamente no BitmapFont (mantido, nao removido).
  O estado PLAYING nunca desenha texto — nem tocado por esta mudanca.

  VALIDACAO EMPIRICA (sem Windows/GPU disponivel neste ambiente):
    - glslc real instalado (apt) — text.vert/text.frag compilam p/ SPIR-V
      sem erros.
    - vulkan.h real instalado (libvulkan-dev via apt) — TextPipeline.cpp e
      FontRenderer.cpp compilam limpos com g++ -Wall -Wextra (zero avisos).
    - stb_truetype.h testado com um pequeno programa C real: baking de
      Orbitron.ttf confirmado (95/96 glyphs nao-vazios), atlas volcado
      para PNG e inspeccionado visualmente (glifos limpos, anti-aliased).
    - stbtt_GetBakedQuad testado empiricamente para confirmar a semantica
      exacta de yoff/y0/y1 antes de escrever a logica de conversao Y-baixo
      -> Y-cima em FontRenderer::drawText.
    - Renderer.cpp e main.cpp (modificados) compilados com sucesso contra
      stubs completos das classes do projecto (Physics, Player, Level,
      Camera, InputManager, Pipeline) replicando as assinaturas reais.
    - Link final de main.cpp+Renderer.cpp+TextPipeline.cpp+FontRenderer.cpp
      contra libvulkan+libglfw reais: sucesso.
    Apesar disto, o comportamento em GPU real (Intel Iris Xe) so pode ser
    confirmado por Rafael — nomeadamente a troca de pipeline a meio do
    render pass e as transicoes de layout de imagem.

  REORGANIZE.PY: rotas novas para TextPipeline.h/.cpp, FontRenderer.h/.cpp,
    text.vert/text.frag, UIFont.ttf (-> Game/Assets/Fonts/), stb_truetype.h
    (-> external/stb/), test_text_pipeline.cpp. DIRS_WITH_GITKEEP:
    Game/Assets/Fonts, external/stb.

  README.md: nova seccao "6.1 Planos Futuros" com as ideias do Rafael
  (botao Sair, menu de pausa via ESC sem terminar a run, botao Creditos
  no(s) menu(s), timer com pausa ao entrar em menu, pasta de runs com
  tempos + identificacao de campanha). Marcadas explicitamente como NAO
  aprovadas — apenas registo, nada implementado.
Implementado: [A confirmar com make tests (esperado 70/70) + make game]

[2026-07-04 14:00:00]
Ideia: v8.1 — Implementar Fase 8.2 completa: menu de pausa via ESC (distinto
  do menu de fim-de-run, confirmado por Rafael), botao "Sair" em ambos os
  menus, "Creditos" acessivel a partir de qualquer um dos dois, timer de
  run que pausa fora de PLAYING, e registo de runs completas com ID
  deterministico de campanha.

  NOVO ESTADO: GameState::PAUSED (Renderer.h/main.cpp).
    - ESC durante PLAYING -> PAUSED (NAO termina a run, NAO mostra ecra de
      "fim de jogo"). ESC de novo -> retoma PLAYING.
    - PAUSED desenha o MUNDO CONGELADO (plataformas+FLAG+jogador, extraido
      para uma lambda drawWorld() partilhada com PLAYING) por baixo de um
      overlay preto semi-transparente (alpha 0.55) + o menu de pausa.
      Reforca visualmente "isto e uma pausa", nao um ecra novo.
    - Opcoes de PAUSED: CONTINUAR / CREDITOS / SAIR (3 caixas, layout
      partilhado com o MENU de fim-de-run para consistencia visual).

  MENU (fim-de-run) ALTERADO: 2 opcoes -> 3 (COMECAR / CREDITOS / SAIR).
    Layout recalculado para 3 caixas de 170px com 18px de gap, centradas.

  NAVEGACAO: wraparound circular via `(menuSel + delta + 3) % 3`, testado
    isoladamente (forward 0->1->2->0, backward 0->2->1->0, ambos OK).

  CREDITOS: agora rastreia `creditsReturnState` (MENU ou PAUSED) — SPACE
    ou ESC dentro de CREDITOS regressam a quem o chamou, em vez de ir
    sempre para MENU como antes.

  TIMER: `elapsedTime` acumulado em main.cpp usando o MESMO dt (chrono) ja
    usado para o loop, mas SO quando state==PLAYING (pausa automaticamente
    em CREDITS/MENU/PAUSED). Desligado da simulacao FIXED_STEP de
    proposito — e um cronometro de parede (wall-clock), nao afecta o
    determinismo da fisica. HUD no canto superior direito durante PLAYING
    (formato MM:SS) — primeira vez que texto e desenhado durante gameplay
    activo (antes so em CREDITS/MENU). Reutiliza o mesmo mecanismo de
    troca de pipeline ja validado.

  NOVO: Game/Core/CampaignID.h (header-only).
    FNV-1a 64-bit sobre campaign.txt + cada .lvl referenciado, na ordem.
    Resolve a questao em aberto do proprio Rafael ("como diferenciar
    campanhas com o mesmo nome, ou versoes diferentes"): qualquer byte
    alterado num nivel, ou na ordem/composicao da campanha, muda o ID de
    16 hex chars. std::hash NAO foi usado — a norma C++ permite-lhe variar
    entre execucoes/compiladores, o que o torna inutil como identificador
    estavel entre maquinas/sessoes.

  NOVO: Game/Logic/RunHistory.h (header-only).
    recordRun(csvPath, campaignName, campaignID, elapsedSeconds) acrescenta
    uma linha a Development/Runs/runs.csv (cria pasta+cabecalho se
    necessario). Colunas: timestamp,campaign_name,campaign_id,
    elapsed_seconds,elapsed_formatted. Formato CSV plano, git-diffable,
    consistente com o estilo ja usado em dev_log.txt.

  TESTES NOVOS (Tests/Unit/):
    test_campaign_id.cpp — 7 testes: determinismo do FNV-1a, hex sempre
      16 chars, campanha inexistente->vazio, mesma campanha->mesmo ID,
      alterar 1 nivel->ID diferente, trocar ORDEM->ID diferente, mesmo
      conteudo (campanhas "gemeas")->mesmo ID.
    test_run_history.cpp — 3 testes: formatacao MM:SS.mmm (incl. clamp de
      negativos), criacao de cabecalho na 1a escrita, append sem duplicar
      cabecalho.
    Total novo: 10 testes. Rodados de facto (nao so compilados) com
    doctest real neste ambiente: 11/11 passed, 28/28 assertions (inclui
    tambem os testes ja existentes que arrastam para o mesmo binario de
    teste isolado usado para validacao).

  VALIDACAO EMPIRICA (mesma disciplina das sessoes anteriores):
    - Renderer.cpp (drawWorld extraido, PAUSED, timer HUD, MENU 3 opcoes)
      compilado com g++ -Wall -Wextra contra vulkan.h real: zero avisos.
    - main.cpp (maquina de estados completa) compilado da mesma forma:
      zero avisos.
    - Link final de main.cpp+Renderer.cpp+TextPipeline.cpp+FontRenderer.cpp
      contra libvulkan+libglfw reais: sucesso.
    - CampaignID.h + RunHistory.h: testes doctest REAIS compilados E
      EXECUTADOS (nao so compilados) — 11/11 passed.
    - RunHistory: verificacao manual do runs.csv gerado (formato correcto,
      cabecalho, timestamp, tempo formatado MM:SS.mmm).
    - navigate() (wraparound do menu): testado isoladamente com asserts.

  REORGANIZE.PY: CampaignID.h -> Game/Core/, RunHistory.h -> Game/Logic/,
    test_campaign_id.cpp + test_run_history.cpp -> Tests/Unit/. Novo
    DIRS_WITH_GITKEEP: Development/Runs/ (onde runs.csv sera criado;
    RunHistory::recordRun() cria a pasta em runtime se nao existir, o
    .gitkeep e so para o git rastrear a pasta vazia antes da 1a run).

  PENDENTE (fora do ambito desta entrega, aguarda decisao de Rafael):
    Fase 9 — Editor de Niveis Visual. Descrito no README apenas como
    "editor com rato, validacao IA automatica em background". Scope
    genuinamente por definir (input de rato ainda nao existe em
    InputManager; layout/toolset do editor; modelo de threading para a
    validacao IA em background sem bloquear o render loop; fluxo de
    guardar/carregar .lvl a partir do editor). Nao implementado sem
    confirmacao — perguntado a Rafael directamente.
Implementado: [A confirmar com make tests -- esperado 80/80: 68 anteriores
  + 2 test_text_pipeline (v7.6) + 7 test_campaign_id + 3 test_run_history]
  + make game (confirmar visualmente PAUSED/MENU/timer HUD/runs.csv)

[2026-07-05 15:00:00]
Ideia: v8.3 — Fix de 3 bugs visuais reportados por screenshot, fix de 2
  testes flaky no Windows, e SpritePipeline+SpriteRenderer (primeiro
  sprite real do jogo — protagonista, a partir de .pixil do Pixilart).

  BUG 1 — Fundo da FLAG com cor estranha (castanho/oliva):
    Causa: zona de touchdown desenhada como rectangulo dourado a 18% de
    alpha sobre o fundo azul-escuro do jogo — a mistura resultava numa
    cor castanha/oliva que parecia um bug. Corrigido: rect removido,
    fica so o mastro + pano (analise coincidiu com a do Gemini).

  BUG 2 — Timer HUD com "texto garbled" ("barra azul a tapar"):
    Causa raiz (identificada por analise geometrica, nao apenas por
    inspeccao visual): baseline do timer estava em LOGICAL_HEIGHT-12=348.
    Com scale=0.6, a altura do glyph (~28*0.6=16.8un) fazia o TOPO do
    texto chegar a ~364.8 -- ULTRAPASSANDO o limite do ecra logico (360).
    O clipping de viewport/NDC cortava o topo de cada digito, dando o
    aspecto de caracteres errados/garbled. NAO era um bug de UV mapping
    ou de push constants (esses ja estavam correctos -- CREDITS, que usa
    a mesma funcao drawText, renderiza perfeitamente, confirmando que o
    problema era espacial/posicional, nao no pipeline de texto em si).
    Corrigido: baseline movido para LOGICAL_HEIGHT-28=332, scale subido
    para 0.7 (melhor legibilidade), margem de 8.4un garantida.
    NOTA: a analise do Gemini sugeria "rectangulo de fundo acidental" ou
    mover para o canto esquerdo -- nenhuma das duas teria corrigido a
    causa raiz (clipping por posicao-Y), so a reposicao vertical resolve.

  BUG 3 — Espacamento no ecra de CREDITOS:
    Causa: labels pequenos (scale~0.55) seguidos de nomes grandes
    (scale~0.85) com baselines demasiado proximos -- o TOPO do nome
    ficava por cima do BASELINE do label acima (crowding). Corrigido:
    layout recalculado por completo (nao so "encolher o texto" como o
    Gemini sugeriu) -- todos os scales e baselines recalculados em
    conjunto para garantir >=8un de folga entre qualquer topo de texto e
    o baseline do texto acima.

  TESTES FLAKY (test_campaign_id.cpp, test_run_history.cpp):
    Causa: std::filesystem::remove_all(dir) [THROWING] podia lancar
    excepcao no Windows quando o SO ainda mantinha um ficheiro
    momentaneamente "bloqueado" apos o ofstream ser fechado (latencia de
    release do handle, comum no NTFS). A excepcao nao apanhada FAZIA
    FALHAR O TESTE mesmo com todas as assercoes reais correctas.
    Corrigido: TODAS as chamadas trocadas para a variante com
    std::error_code (nao-throwing) -- uma falha de limpeza transitoria
    deixa (raramente) um residuo inofensivo, mas NUNCA crasha o teste.
    ORGANIZACAO (pedido de Rafael: "junta as temporarias runs numa pasta
    so"): pastas de teste que viviam soltas na raiz do projecto
    (tmp_campaign_a, tmp_runs_test_a, etc. -- ficavam la quando um teste
    falhava a meio, antes da linha de limpeza) foram TODAS consolidadas
    em build/test_tmp/<nome_do_teste>/. build/ ja esta gitignored e e'
    removido por 'make clean' -- limpa automaticamente qualquer residuo
    sem sujar a raiz do repositorio.

  SPRITEPIPELINE + SPRITERENDERER (protagonista em pixel-art real):
    Rafael enviou ascended__1_.pixil (formato JSON do Pixilart, NAO um
    ZIP). Ficheiro tinha uma sequencia de "lixo" tipo keyboard-mash
    ("98kjasdnasd983/24kasdjasd") inserida exactamente entre "image/png"
    e "base64," em DOIS sitios do JSON (preview + layer src) -- nao
    parecia corrupcao aleatoria (posicao identica nos dois casos), mas
    tambem nao teve qualquer efeito para alem de quebrar a sintaxe do
    data URI; tratado como corrupcao de export, removido com regex antes
    de descodificar. Resultado: sprite 32x32 RGBA valido, 7 cores unicas,
    236 pixeis transparentes / 788 opacos (transparencia real confirmada).

    ARQUITECTURA: SpritePipeline+SpriteRenderer espelham a estrutura de
    TextPipeline+FontRenderer (mesma pipeline dedicada com descriptor set
    + mesmo padrao de upload via staging buffer), com duas diferencas
    funcionais: (1) sampler NEAREST em vez de LINEAR -- CRITICO para
    pixel art, preserva arestas nitidas dos pixeis; LINEAR borraria o
    sprite; (2) fragment shader amostra RGBA directamente (sem recolorir
    como o texto faz a partir de um atlas R8 de 1 canal). sprite.vert
    suporta flipX (espelha o sprite horizontalmente via UV, usado para
    facingDirection do jogador sem precisar de uma segunda imagem).
    stb_image.h (mesma familia/licenca do stb_truetype ja usado) para
    descodificar o PNG -- testado a decodificar o sprite com resultado
    byte-a-byte identico ao esperado.

    Renderer::attachSprite() opcional (mesmo padrao de attachText()) --
    sem sprite anexado, o jogador continua a ser um rectangulo solido
    (fallback, zero regressao).

  OPTIMIZACAO DE ESPACO (novo requisito PERMANENTE, pedido por Rafael):
    "o motivo de estar a minha propria engine e fazer um jogo tao simples
    e com pixelarte e optimizar espaco mesmo" -- adicionado ao README como
    secção 10, principio orientador cumulativo (nao bloqueante, "pode se
    ir fazendo").
    DECISAO TESTADA EMPIRICAMENTE (nao assumida): comparei PNG RGBA vs PNG
    indexado (paleta exacta das 7 cores) para o sprite -- SEM optimizar,
    o indexado ficou MAIOR (976B vs 462B, overhead da tabela de paleta
    nao compensa a um tamanho tao pequeno). DEPOIS de 'optipng -o7' em
    AMBOS: convergem para o MESMO tamanho optimo (241 bytes) -- confirma
    que a escolha manual de formato (RGBA vs indexado) e' irrelevante se
    houver um bom optimizador PNG a seguir; a decisao certa e' manter PNG
    simples + sempre correr optipng, nao complicar com formatos
    alternativos. Verificado byte-a-byte (memcmp) que a versao optimizada
    e' 100% identica a original -- zero perda confirmada.
    reorganize.py agora converte .pixil->PNG automaticamente e corre
    optipng se disponivel (fallback gracioso se nao instalado -- nao
    trava o resto do reorganize.py).

  REORGANIZE.PY -- conversao .pixil automatica:
    Novo: PIXIL_NAME_MAP (nomes conhecidos -> caminho semantico esperado
    pelo motor, ex. "ascended"->"player"), convert_pixil_to_png() (extrai
    layer unica directamente -- zero dependencias; multi-layer usa
    Pillow se disponivel, com aviso gracioso se nao estiver), limpeza de
    sufixos de duplicado no nome do ficheiro ("ascended__1_" ->
    "ascended", "ficheiro (2)" -> "ficheiro"). .pixil original preservado
    em Game/Assets/Sprites/Source/ para reedicao futura. Testado
    end-to-end com o ficheiro real enviado -- PNG final confirmado
    byte-a-byte identico a extraccao manual.
    Novos DIRS_WITH_GITKEEP: Game/Assets/Sprites/, Game/Assets/Sprites/Source/.

  VISAO FUTURA registada no README (Fase 9, NAO implementada, posterior
  ao jogo estar jogavel, per Rafael): editor de niveis deve incluir
  escolha de sprites diferentes (chao/plataforma/decoracao), criacao e
  edicao de sprites, regras para troca de sprites.

  VALIDACAO EMPIRICA (disciplina identica a sessoes anteriores):
    - text_campaign_id.cpp/test_run_history.cpp: recompilados e
      re-executados (nao so compilados) -- 11/11 passed apos o fix.
    - SpritePipeline.cpp/SpriteRenderer.cpp: compilados com g++ -Wall
      -Wextra contra vulkan.h real (1 bug apanhado e corrigido: falta de
      #include <vector> em SpriteRenderer.h).
    - sprite.vert/sprite.frag: compilados com glslc real, zero erros.
    - Renderer.cpp/main.cpp (com sprite+fixes): recompilados, zero avisos.
    - Link final de todos os .o (main+Renderer+TextPipeline+FontRenderer+
      SpritePipeline+SpriteRenderer) contra libvulkan+libglfw reais:
      sucesso.
    - .pixil: JSON parseado e inspeccionado directamente (nao assumido);
      garbage no data URI identificado e removido com regex ANTES de
      qualquer decode; sprite final verificado byte-a-byte (memcmp)
      identico em 3 pontos da pipeline (extraccao manual -> stb_image ->
      reorganize.py end-to-end).

  NOTA DE VERSIONAMENTO: ficheiros individuais neste lote mostram v8.2 no
  seu proprio historico incremental (7.6/8.1->8.2); o marco do projecto
  como um todo passa a 8.3 no README (8.2 ja estava atribuido ao
  "Menu reestruturado" de uma sessao anterior).
Implementado: [A confirmar com make tests -- esperado 82/82] + make game
  (confirmar visualmente: FLAG sem cor estranha, timer legivel, creditos
  bem espacados, jogador aparece como sprite pixel-art)

[2026-07-06 10:00:00]
Ideia: v8.3.1 — Fix: sprite nao carregado (nome de ficheiro nao correspondia
  ao esperado) + documentar plano de Release Build (Fase 10, definido por
  Rafael) no README.

  DIAGNOSTICO DO "nao usou o sprite": NAO era um bug de logica no
  reorganize.py. O ficheiro `.pixil` de Rafael chama-se "personagem.pixil"
  (nao "ascended" como no exemplo anterior desta sessao) -- a conversao
  generica correu bem e produziu "Game/Assets/Sprites/personagem.png"
  exactamente como desenhado. O problema real: main.cpp e SpriteRenderer.h
  tinham "player.png" hardcoded como nome esperado -- um mapeamento
  semantico fragil (PIXIL_NAME_MAP com "ascended"->"player") que so
  funcionava para o nome de ficheiro exacto que eu tinha adivinhado antes.

  CORRECAO ARQUITECTURAL (nao um patch pontual): removido o mapeamento de
  sinonimos do reorganize.py -- a conversao passa a ser SEMPRE generica e
  previsivel (nome.pixil -> nome.png, sem adivinhar papeis semanticos).
  main.cpp passa agora EXPLICITAMENTE "Game/Assets/Sprites/personagem.png"
  a playerSprite.init() -- main.cpp e' a fonte da verdade sobre qual PNG
  representa o jogador, nao uma tabela de sinonimos no reorganize.py que
  teria de ser mantida para cada nome novo que Rafael escolhesse (chao,
  plataforma, decoracao, etc. seriam o mesmo problema noutro dia).
  test_sprite_pipeline.cpp tambem actualizado para passar o caminho
  explicitamente (nao depender do valor por omissao da classe, que ja
  mudou uma vez).

  BONUS: reorganize.py ganhou uma passagem de optimizacao retroactiva --
  corre optipng sobre TODOS os sprites existentes em Game/Assets/Sprites/
  a cada execucao (nao so os recem-convertidos). Se optipng for instalado
  DEPOIS de sprites ja convertidos sem ele, basta correr reorganize.py de
  novo para aplicar a optimizacao, sem reconverter nada a mao.

  make shaders / sprite.vert+frag: 'make shaders' reportou "Nothing to be
  done" (mesmo problema que ja tinha acontecido com text.vert/frag numa
  sessao anterior). Sem visibilidade do Makefile actual de Rafael, a
  solucao pragmatica continua a ser compilacao manual:
    glslc Game/Assets/Shaders/sprite.vert -o Game/Assets/Shaders/sprite.vert.spv
    glslc Game/Assets/Shaders/sprite.frag -o Game/Assets/Shaders/sprite.frag.spv
  NOTA PARA MAIS TARDE: o Makefile provavelmente precisa de um alvo
  'shaders' que faca glob de TODOS os .vert/.frag em vez de uma lista
  fixa -- mas isso exige ver o Makefile real, que nao esta disponivel
  neste momento.

  RELEASE BUILD (Fase 10) -- REGISTO DE PLANO, NADA IMPLEMENTADO:
  Rafael definiu 5 regras obrigatorias para a distribuicao final do jogo
  (executavel portable, standalone, sem terminal visivel, sem tocar fora
  da propria pasta, falha graciosa com MessageBox nativa se faltarem
  requisitos como suporte Vulkan, README separado Dev/Jogador).
  Documentado integralmente no README como nova "Fase 10" no roadmap
  (secção 6), com tabela detalhando cada regra + notas tecnicas onde ja
  ha ligacao clara ao codigo existente:
    - Regra 4 (falha graciosa): VulkanContext::init() ja devolve bool,
      mas main.cpp actual NAO verifica esse retorno -- ponto de entrada
      natural para a futura checagem + MessageBoxW, identificado mas
      NAO implementado agora (Rafael foi explicito: "um dos ultimos
      passos", nao bloqueante para o resto do roadmap).
    - Regra 1 (sem terminal): sinalizada a incerteza sobre qual flag de
      linker se aplica (-mwindows estilo MinGW vs /SUBSYSTEM:WINDOWS
      estilo MSVC) dado que o projecto usa Clang a atingir
      x86_64-pc-windows-msvc mas compila via GNU Make/Git Bash -- por
      confirmar quando chegar a altura, nao assumido sem testar.
  Secção 10 (Optimizacao de Espaco) recebeu uma nota cruzada para a Fase
  10, ja que "pacote portable, nada instalado" e' a expressao mais visivel
  do mesmo principio de minimizar footprint.
Implementado: [A confirmar com make tests -- esperado 82/82, sem alteracao
  de contagem nesta sessao, so fixes] + make game (confirmar visualmente
  que o jogador aparece como personagem.png)

[2026-07-11 22:30:00]
Ideia: v9.1 — Fase 9.1: Sistema de Configuracao de Controlos (GameAction + KeyBindings).
  Objectivo: substituir os `input.isKeyJustPressed(Key::X)` hardcoded em
  main.cpp por uma camada logica de accoes, reatribuivel e persistivel, e
  abrir caminho para o ecra CONTROLS (lista + remapeamento) e para a tecla
  dedicada do Editor de Niveis (Fase 9.3).
  GameAction.h/.cpp (Game/Core/): enum com 9 accoes (MoveLeft, MoveRight,
    Jump, Pause, UIConfirm, UILeft, UIRight, OpenEditor, Quit) + nome PT
    (UI) e nome serializado estavel (ficheiro de config), com conversao
    reversivel nos dois sentidos.
  KeyBindings.h/.cpp (Game/Core/): mapeia GameAction -> vector<int> (varias
    teclas por accao, para nao perder o A+LEFT/D+RIGHT que isLeft()/
    isRight() ja suportam). Persistencia em texto simples
    (Development/Settings/controls.cfg, formato ACCAO=TECLA1,TECLA2).
    Fallback numerico para qualquer codigo GLFW sem nome na tabela.
    Adaptadores isActionHeld/isActionJustPressed/isActionJustReleased
    fazem OR entre as teclas associadas, consultando o InputManager real.
Implementado: 29 testes novos (test_game_action.cpp: 8, test_keybindings.cpp:
  21) | 211 assertions | 100% a passar -- compilados e corridos de verdade
  na sandbox com clang++ 18 (Ubuntu) contra InputManager.h/.cpp REAIS
  (pedidos e recebidos do Rafael nesta sessao), nao assumidos.

  DESCOBERTA IMPORTANTE: logic::Key NAO e' um enum -- e' um namespace de
  `constexpr int` (codigos GLFW directos: SPACE=32, A=65, etc.), e todos os
  metodos de InputManager (isKeyDown, isKeyJustPressed, isKeyJustReleased)
  recebem `int`, nao um tipo customizado. A suposicao inicial de trabalho
  (enum class) teria produzido uma KeyBindings incompativel -- so nao
  aconteceu porque o ficheiro real foi pedido e lido antes de escrever
  qualquer coisa que dependesse dele. KeyBindings.h documenta isto.

  Key::E (69) e Key::Q (81) adicionados ao namespace Key em InputManager.h
  -- adicao pura (nenhum valor/assinatura existente alterado), sao os
  defaults de OpenEditor e Quit, accoes que nao tinham tecla nomeada antes.
  Confirmado que Physics.cpp/Player.cpp/Level.cpp/ReplayManager.cpp (todos
  incluem InputManager.h) continuam a compilar sem alteracoes.

  main.cpp (v9.1): Pause/UIConfirm/UILeft/UIRight deixam de verificar
  Key::ESCAPE/LEFT/RIGHT/SPACE directamente -- passam por
  core::isActionJustPressed(bindings, input, GameAction::X). Nova accao
  Quit (default Q) sai do jogo directamente a partir de PAUSED/MENU, sem
  precisar navegar ate "Sair" + confirmar. bindings.loadFromFile(
  "Development/Settings/controls.cfg") na iniciacao, com o mesmo fallback
  gracioso ja usado para a fonte TTF/sprite (ficheiro ausente -> defaults,
  identicos ao comportamento anterior a esta sessao). Variavel local
  `escPressed` renomeada para `pausePressed` (deixou de ser literalmente
  "ESC" assim que se tornou reatribuivel).

  AMBITO DELIBERADAMENTE LIMITADO: MoveLeft/MoveRight/Jump existem na
  tabela de accoes (para o futuro ecra CONTROLS listar tudo) mas NAO estao
  ligadas ao gameplay real ainda -- Player.cpp continua a chamar
  input.isLeft()/isRight()/isKeyDown(Key::SPACE) directamente (metodos
  hardcoded dentro do proprio InputManager). Ligar estas tres a
  KeyBindings exige tocar em Player.cpp, ficheiro testado e estavel --
  fica pendente de confirmacao explicita antes de mexer.

  VALIDACAO EMPIRICA (alem da compilacao isolada): main.cpp completo
  compila (clang++ -c, headers Vulkan/GLFW reais instalados via apt) E o
  motor INTEIRO liga sem erros (main.cpp + 11 ficheiros Graphics/*.cpp + 4
  Logic/*.cpp + GameAction.cpp + KeyBindings.cpp, contra libvulkan e
  libglfw3 reais do sistema) -- binario ELF de 548KB produzido. Nao corre
  aqui (sem GPU/display na sandbox Linux, e o alvo real e' Windows/Clang22/
  MSVC-ABI, ambiente diferente), mas a validacao sintactica e de simbolos
  cobre o ficheiro inteiro, nao so o excerto alterado.

  reorganize.py: rotas novas para GameAction.h/.cpp, KeyBindings.h/.cpp,
  test_game_action.cpp, test_keybindings.cpp + pasta Development/Settings/
  (onde controls.cfg vai ficar, gerado por uma futura UI CONTROLS -- por
  agora so o .gitkeep). Testado de verdade: replica minima da raiz com os
  6 ficheiros novos, reorganize.py corrido, confirmados todos nos destinos
  correctos.

  Config.h: EDITOR_GRID_SNAP = 4.0f adicionado (Fase 9.4 -- pedido
  explicito para testar grelha fina antes de decidir entre 4 e 16). Nao
  consumido ainda por nada; um so ponto de mudanca quando o Editor
  precisar dele.

  PENDENTE (nao implementado nesta sessao):
    - Ecra CONTROLS (listar + remapear "prime uma tecla") -- precisa de
      rato (Fase 9.2) e wiring ao Renderer/TextPipeline.
    - Ligar MoveLeft/MoveRight/Jump a KeyBindings (exige tocar Player.cpp,
      pendente de confirmacao).
    - GameAction::OpenEditor nao e' consumido em lado nenhum ainda --
      GameState::EDITOR so existe a partir da Fase 9.3.
    - README.md: NAO actualizado nesta entrada. Pedido o ficheiro actual
      a Rafael antes de o tocar -- este dev_log regista que uma sessao
      anterior ja adicionou a Fase 10 ao README (nao visivel nesta
      janela), e reescrever a partir da copia desactualizada disponivel
      aqui apagaria esse trabalho. Fica para assim que o ficheiro chegar.

[2026-07-12 10:00:00]
Ideia: v9.1a — Fix: test_keybindings.cpp falha no Windows (exception no remove()).
  Rafael correu `make tests-verbose -j8 > Tests/log_testes.txt 2>&1` no ambiente
  real (Windows/Clang22) -- 109/110 test cases, 485/485 assertions. O unico
  test case a falhar nao tinha NENHUMA assertion falhada; a falha era uma
  excecao nao apanhada, sinal classico de um problema de ambiente e nao de
  logica: "test_keybindings.cpp(221): ERROR: test case THREW exception:
  remove: The process cannot access the file because it is being used by
  another process."
Implementado: Causa localizada por inspeccao directa da linha 221 (TEST_CASE
  "saveToFile: grava todas as ALL_ACTIONS, uma linha cada"): um
  `std::ifstream f(path)` era aberto para reler o ficheiro gravado, lido via
  `std::getline` num ciclo, mas NUNCA fechado explicitamente antes do
  `std::filesystem::remove(path)` no fim do mesmo TEST_CASE.
  No Linux (onde este ficheiro foi validado antes de entregar) isto passa
  despercebido -- POSIX permite apagar um ficheiro com um handle ainda
  aberto (unlink nao bloqueia). No Windows, a implementacao por omissao de
  ifstream nao abre com FILE_SHARE_DELETE, por isso remove() falha com
  "used by another process" enquanto o handle existir.
  Fix: `f.close()` explicito a seguir ao ciclo de leitura, antes do
  `CHECK` e do `remove()`. Auditados TODOS os outros std::ifstream/
  std::ofstream deste ficheiro (grep directo) -- os restantes 3 ja fechavam
  explicitamente antes de qualquer remove(); este era o unico caso.
  Recompilado e re-corrido no Linux (29/29, 211/211 -- inalterado, como
  esperado, ja que o Linux nunca teve o bug). A confirmacao real fica para
  o proximo `make tests-verbose` no ambiente Windows do Rafael.
  LICAO: ao escrever testes que abrem um ficheiro para leitura/escrita e
  depois o removem na mesma funcao/scope, fechar SEMPRE o stream
  explicitamente antes do remove() -- o sandbox Linux usado para validar
  antes de entregar nao apanha esta classe de bug, so o ambiente Windows
  real o revela. Mesma familia de problema ja visto antes em
  test_campaign_id.cpp/test_run_history.cpp (remove_all a lancar).

[2026-07-13 15:00:00]
Ideia: v9.2 — Fase 9.2: Rato (InputManager + conversao de coordenadas + MENU/PAUSED clicaveis).
  Objectivo: InputManager ganha posicao do cursor + botoes (mesmo padrao
  current/justDown/justUp ja usado para teclado), e MENU/PAUSED passam a
  aceitar clique esquerdo nas 3 caixas -- confirmado por Rafael: "aproveita
  para adicionar rato ao menu (e ser interativo)".
  InputManager.h/.cpp: MouseButton::LEFT/RIGHT/MIDDLE (namespace, mesma
    filosofia de Key::), onMouseButtonEvent/onCursorPosEvent + callbacks
    GLFW (glfwSetMouseButtonCallback/glfwSetCursorPosCallback), buffers
    SEPARADOS dos de teclado (m_mouseCurrent/...) -- zero risco de colisao
    de codigos entre tecla e botao. beginFrame() agora tambem limpa os
    buffers de rato. injectCursorPos() para testes/mocks. Tudo aditivo --
    nenhum metodo/campo existente alterado.
  Viewport.h/.cpp (Game/Core/, NOVO): windowToLogical() converte cursor
    (pixeis de janela, Y-baixo) para espaco logico do motor (Y-cima,
    mesma convencao de Level::addPlatform). Formula EXTRAIDA por leitura
    directa de Renderer::recordCommandBuffer (linhas ~169-184) -- mesmo
    calculo de letterbox, mesma inversao de Y que base.vert ja faz para
    UI sem camara. hitTestMenuBox() + MenuBoxLayout: geometria das 3
    caixas (BOX_W=170, BOX_H=62, BOX_Y=150, GAP=18) tambem extraida por
    leitura directa de Renderer.cpp -- confirmado que PAUSED e MENU usam
    EXACTAMENTE os mesmos valores entre si, por isso um unico hit-test
    serve os dois estados.
Implementado: 22 testes novos (test_viewport.cpp: 15, test_input_mouse.cpp:
  7) | 61 assertions | 100% a passar -- compilados e corridos de verdade.
  Total combinado desde a Fase 9.1: 51 test cases, 272 assertions.

  DECISAO DELIBERADA: Renderer.cpp NAO foi tocado. O calculo de letterbox
  e a geometria das caixas continuam inline la dentro (e ja duplicados
  entre si -- PAUSED e MENU tinham cada um a sua copia antes desta sessao).
  Viewport.h/.cpp e' uma TERCEIRA copia dos mesmos valores, comentada a
  apontar exactamente para onde manter em sincronia se Renderer.cpp mudar.
  Preferi nao refactorizar codigo grafico estavel sem confirmacao explicita
  -- se quiseres unificar as 3 copias numa fonte unica (Renderer.cpp passa
  a chamar Viewport::computeLetterbox() em vez de calcular inline), e' um
  follow-up pequeno e facil de aprovar depois.

  main.cpp (v9.2): novo lambda clickedMenuBox() (junto a navigate()) --
  devolve o indice da caixa sob o cursor se o botao esquerdo acabou de ser
  premido, ou -1. PAUSED e MENU: clique numa caixa faz menuSel=indice E
  entra no mesmo bloco de UIConfirm na mesma frame (comportamento standard
  de botao -- nao precisa de um segundo clique/tecla para confirmar).
  CREDITS continua so' por teclado (nao pedido explicitamente; extensao
  trivial se for preciso).

  test_input_mouse.cpp criado como ficheiro NOVO em vez de estender
  test_input.cpp -- este ultimo nunca foi partilhado nesta sessao, e
  editar um ficheiro que nao vi arriscava sobrepor/perder testes reais.

  VALIDACAO EMPIRICA: main.cpp compila isolado (clang++ -c, headers
  Vulkan/GLFW reais) E o motor INTEIRO liga sem erros (main.cpp + 11
  Graphics/*.cpp + 4 Logic/*.cpp + GameAction/KeyBindings/Viewport.cpp,
  contra libvulkan/libglfw3 reais) -- binario de 549KB. Confirmados
  tambem, individualmente, que Physics/Player/Level/ReplayManager.cpp
  continuam a compilar sem alteracoes apos o InputManager.h crescer.

  reorganize.py: rotas novas para Viewport.h/.cpp, test_viewport.cpp,
  test_input_mouse.cpp. Testado de verdade (replica minima da raiz,
  ficheiros nos destinos correctos).

  PENDENTE (nao implementado nesta sessao):
    - CREDITS nao aceita clique (so' teclado) -- nao pedido, facil de
      adicionar se for preciso.
    - Ecra CONTROLS (Fase 9.1 pendente) pode agora usar o mesmo
      clickedMenuBox()/hitTestMenuBox() assim que existir uma lista de
      accoes em vez de 3 caixas fixas -- hitTestMenuBox() esta escrito
      para MenuBoxLayout::COUNT=3 especificamente; generalizar para N
      linhas fica para quando o ecra CONTROLS for implementado.
    - MoveLeft/MoveRight/Jump continuam por ligar a Player.cpp (mesma
      nota da Fase 9.1, ainda pendente de confirmacao).

[2026-07-13 17:30:00]
Ideia: v9.2a — Fix CRITICO de infraestrutura: Makefile nao rastreava dependencias de headers.
  Rafael correu `make tests-verbose -j8 > Tests/log_testes.txt 2>&1` apos a
  Fase 9.2 -- SIGSEGV em test_input.cpp(34) ("beginFrame limpa justPressed e
  justReleased..."), logo a seguir a um CHECK que passou. 37/38 test cases,
  94 "skipped" (consequencia do crash a meio da execucao, nao de um filtro).
Implementado: Causa raiz identificada por leitura do proprio log de build:
  "[CC ] Game/Logic/InputManager.cpp" aparece, mas "[CC ] Tests/Unit/
  test_input.cpp" NAO aparece -- ou seja, test_input.cpp usou um .o
  ANTIGO da build anterior, compilado contra a versao de InputManager.h
  de ANTES da Fase 9.2 (sem m_mouseCurrent/m_mouseJustDown/m_mouseJustUp/
  m_cursorX/m_cursorY). InputManager.cpp foi recompilado contra a versao
  NOVA (com esses membros). O binario final ligou objectos com duas nocoes
  diferentes de sizeof(InputManager) -- ODR violation classico. O
  beginFrame() novo tenta fazer .clear() em m_mouseJustDown/m_mouseJustUp,
  que nao existem no objecto que test_input.cpp alocou (mais pequeno) ->
  acesso fora dos limites -> SIGSEGV.
  Porque so' apareceu agora: a Fase 9.1 tambem tocou InputManager.h
  (Key::E, Key::Q), mas eram CONSTANTES DE NAMESPACE, nao membros da
  classe -- nao mudam sizeof(InputManager), por isso nao ha incompatibilidade
  de ABI possivel. A Fase 9.2 foi a primeira vez que um membro de classe
  foi adicionado, e' por isso que so' agora rebentou.
  RAIZ: a regra generica `$(BUILD_DIR)/%.o: %.cpp` do Makefile so' depende
  do proprio .cpp -- nunca dos headers que ele inclui. `make` nao tinha
  como saber que test_input.cpp precisava de recompilar so' porque
  InputManager.h mudou.
  FIX: `-MMD -MP` adicionado a CXXFLAGS_BASE (gera um ficheiro .d ao lado
  de cada .o, listando os headers do projecto de que depende) + `-include
  $(DEPS)` no Makefile (faz o make ler esses .d e tratar headers como
  dependencias de verdade). Validado empiricamente em dois niveis: (1)
  exemplo isolado minimo, confirmando que tocar so' num header dispara
  recompilacao do .cpp dependente; (2) replica real com GameAction/
  KeyBindings/Viewport/InputManager/Physics/Player/Level/ReplayManager e
  os 4 ficheiros de teste desta sessao -- 51/51, 272/272 -- e depois
  simulando exactamente o cenario da Fase 9.2 (adicionar um membro a
  InputManager.h sem tocar em nenhum .cpp): confirmado que Player.cpp,
  ReplayManager.cpp, InputManager.cpp e os testes relevantes recompilam
  todos correctamente na proxima `make`, sem `make clean` manual.
  Desbloqueio imediato para Rafael: `make clean` (forca tudo a recompilar
  de raiz, resolve o sintoma agora). Este fix resolve a CAUSA -- nunca
  mais devia ser preciso `make clean` so' por causa de um header ter
  mudado.
  LICAO (grave-se ao lado da de test_keybindings v9.1a): headers amplamente
  incluidos que ganham/perdem MEMBROS DE CLASSE (nao apenas constantes)
  sao o caso de risco real para ABI mismatch entre unidades de traducao
  compiladas em momentos diferentes. Antes deste fix, qualquer mudanca
  destas exigia `make clean` manual para ser segura; a partir de agora,
  o proprio `make` trata disso.

[2026-07-13 18:00:00]
Ideia: v9.2a — Confirmacao final apos fix do Makefile.
Implementado: `make clean && make tests-verbose -j8` corrido por Rafael com
  o Makefile corrigido (-MMD -MP). Resultado: 132/132 test cases, 546/546
  assertions, SUCCESS. Bate certo exactamente com o numero previsto (Fase
  9.1: 110 + Fase 9.2: 22 = 132; 485+61=546). SIGSEGV de test_input.cpp
  confirmado resolvido -- nao voltou a ocorrer numa build limpa com o fix
  de dependencias de headers em vigor. Fase 9.1 e 9.2 dao-se por fechadas
  e confirmadas em execucao real.
